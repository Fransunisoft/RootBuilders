import PagePanel from "@/components/ui/PagePanel";

export default function AdminTasksPage() {
  return (
    <PagePanel
      title="Tasks"
      description="Create weekly tasks, review submissions, and trigger activity updates for the cohort."
    />
  );
}
