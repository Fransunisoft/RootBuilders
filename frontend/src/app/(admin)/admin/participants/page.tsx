import PagePanel from "@/components/ui/PagePanel";

export default function AdminParticipantsPage() {
  return (
    <PagePanel
      title="Participants"
      description="View, search, filter, assign teams, and monitor participant engagement across the active cohort."
    />
  );
}
