import PagePanel from "@/components/ui/PagePanel";

export default function AdminSettingsPage() {
  return (
    <PagePanel
      title="Settings"
      description="Manage dashboard configuration, super admin access, and participant team lead assignments."
    />
  );
}
