import PagePanel from "@/components/ui/PagePanel";

export default function AdminTeamsPage() {
  return (
    <PagePanel
      title="Teams"
      description="Create teams, assign a participant as team lead, and monitor team health from the super admin workspace."
    />
  );
}
