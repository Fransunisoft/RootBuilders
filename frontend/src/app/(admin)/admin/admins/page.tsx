import PagePanel from "@/components/ui/PagePanel";

export default function AdminsPage() {
  return (
    <PagePanel
      title="Admins"
      description="Invite other admins and manage full system access from the super admin workspace."
    />
  );
}
