import PagePanel from "@/components/ui/PagePanel";

export default function ParticipantTasksPage() {
  return (
    <PagePanel
      title="My Tasks"
      description="View weekly tasks, deadlines, and submission requirements for your participant workspace."
    />
  );
}
